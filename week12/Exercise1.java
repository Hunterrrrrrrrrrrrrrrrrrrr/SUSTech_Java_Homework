package week12;

import java.util.ArrayList;

public class Exercise1 {
  public static void main(String[] args) {
    /*
    ArrayList<Monkey> mArray = new ArrayList<Monkey>();
    for(int i = 0; i < 6; i ++)
      if(i % 2 == 1)
        mArray.add(new Human());
      else
        mArray.add(new Monkey());
    for(Monkey monkey: mArray)
      monkey.speak();
      */
    
  }
}
